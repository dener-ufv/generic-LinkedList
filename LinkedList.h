#ifndef LINKED_LIST_H
#define LINKED_LIST_H

/*

#include "Data.h"

typedef struct linkedList_t * LinkedList;
typedef unsigned int uint;

struct linkedList_t {
  Data data;
  uint dataLength;
  uint listSize;
};

int LinkedListEmpty(LinkedList llist);
int LinkedListInsert(LinkedList llist, void *item, int size);
int LinkedListInit(LinkedList llist);
//int LinkedListRemove(LinkedList llist, uint index);
//int LinkedListSize(LinkedList llist);

*/

#endif